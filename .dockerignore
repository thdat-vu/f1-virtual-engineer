# Source-control + editor
.git
.gitignore
.github
.idea
.vscode
.codex
.claude
**/.DS_Store

# Docs and ad-hoc artefacts
README.md
*.md
docs/

# Backend — keep source/runtime, drop everything else
backend/.env
backend/.env.*
!backend/.env.example
backend/data/
backend/tests/
backend/.pytest_cache/
backend/notebooks/
backend/eval/
backend/evals/
# Re-include the strategy eval status JSON — frontend Docker build
# reads it at `next build` time so the landing page renders the
# same number CI enforces. Must be listed AFTER the `backend/evals/`
# rule above for the un-ignore to take effect.
!backend/evals/cases/
!backend/evals/cases/strategy_pit_status.json
backend/migrations/
backend/scripts/
backend/infra/
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Frontend — drop installed deps + build artefacts so the build is hermetic
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/coverage/
frontend/.env
frontend/.env.*
!frontend/.env.local.example
!frontend/.env.example
frontend/.vercel/
frontend/*.tsbuildinfo
frontend/next-env.d.ts

# Compose stays out of the build context; Dockerfiles must remain
# inside it so `docker build -f backend/Dockerfile .` can find them.
docker-compose*.yml
